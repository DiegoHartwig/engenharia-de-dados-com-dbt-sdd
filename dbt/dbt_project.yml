name: engenharia_de_dados_com_dbt_sdd
version: "1.0.0"
config-version: 2

profile: engenharia_de_dados_com_dbt_sdd

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  engenharia_de_dados_com_dbt_sdd:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
